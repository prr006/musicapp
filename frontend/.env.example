# Public URL only — never put provider, database, or signing secrets in Vite.
# Local development normally uses /api/v1 through the Vite reverse proxy.
VITE_MELO_API_URL=/api/v1

# Vercel example:
# VITE_MELO_API_URL=https://your-melo-api.up.railway.app/api/v1
