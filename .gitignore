# build output
build/bin/
frontend/dist/*
!frontend/dist/.gitkeep
frontend/node_modules/
frontend/wailsjs/
*.tsbuildinfo

# local tooling
.env.local
.env.*.local

# end-to-end run artifacts (evidence copies live in docs/evidence)
frontend/e2e/

# Pages build output (published to the gh-pages branch, not tracked)
frontend/dist-pages/
