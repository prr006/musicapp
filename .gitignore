node_modules/
dist/
*.local
.DS_Store

# Rust
target/
src-tauri/gen/schemas/

# Logs / coverage
coverage/
*.log

# mobile icons not needed (desktop-first)
src-tauri/icons/android/
src-tauri/icons/ios/

# Managed runtime (libmpv-2.dll / yt-dlp.exe / 7zr.exe) is downloaded and
# verified at runtime — never committed. The dev-mode cache lives at the
# repo root, outside every dev-watcher path, so `tauri dev` never restarts
# mid-bootstrap.
/.melo-runtime/
